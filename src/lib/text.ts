/**
 * Δένει τον διαχωριστή «·» με τη λέξη που ακολουθεί, ώστε να μη μένει
 * ποτέ στο τέλος μιας σειράς όταν το κείμενο αναδιπλώνεται.
 *   «Παιδιών · Εφήβων» → σπάει πριν το «·», όχι μετά.
 */
export function keepSeparators(text: string): string {
  return text.replaceAll(' · ', ' · ');
}
